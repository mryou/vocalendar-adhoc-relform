以下の内容で登録を受け付けました。ありがとうございました。

<%= RelForm::REL_FIELDS.map {|f| "#{field_label f}: #{relinfo.send(f)}" }.join("\n") %>


-------------------------------------------------
      VOCALENDAR  http://vocalendar.jp/
-------------------------------------------------
